#include<stdio.h>
main()
{
    int a;
    for(a=1;a<=9;a++)
    {
        if(a*a<=9) //Find those numbers which will squire itself and will be printed if the squire number is equal or less than 9
        {
            printf("%d\n",a);
            //break;
        }

    }
}
